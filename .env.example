# Environment configuration for the scraper platform

# Scheduler configuration
SCHEDULER_MODE=scheduler
# SCHEDULER_MODE=legacy  # Use this for old behavior

# Discord bot configuration (optional)
# DISCORD_TOKEN=your_discord_bot_token_here

# Database configuration
SCHEDULER_DB_URL=sqlite:///scheduler_jobs.db

# Timezone for scheduling
SCHEDULER_TIMEZONE=Europe/Stockholm

# Pipeline configuration file
PIPELINES_CONFIG=pipelines_scheduled.yml
